# Timeline 深色模式优化指南

## 🌙 设计目标

Timeline 组件在 **深色模式** (`bg-gray-800`) 环境中使用，需要：
- 与深色背景协调统一
- 保持高对比度和可读性
- 增强立体感和层次感
- 提供丰富的视觉反馈

---

## 🎨 深色模式配色方案

### 主色调
```css
/* 容器背景 */
background: linear-gradient(to bottom, #1f2937, #111827)

/* 轨道背景 */
background: linear-gradient(to bottom, #374151, #1f2937)

/* 边框颜色 */
border: #4b5563

/* 文字颜色 */
color: #d1d5db (浅灰)
```

### 强调色
```css
/* 蓝色高亮 */
primary: #60a5fa → #3b82f6

/* 红色播放头 */
playing: #ef4444 → #dc2626

/* 绿色成功 */
success: #10b981

/* 黄色警告 */
warning: #f59e0b
```

---

## 📊 优化前后对比

### 1. 容器背景

#### 优化前
```
┌─────────────────────────────────────┐
│ [无背景，融入页面]                  │
│                                     │
└─────────────────────────────────────┘

问题：
❌ 无边界感
❌ 无层次感
❌ 无立体感
```

#### 优化后
```
╔═════════════════════════════════════╗
║ [深色渐变 + 内部高光 + 阴影]       ║
║                                     ║
╚═════════════════════════════════════╝

改进：
✅ 深色渐变背景 (#1f2937 → #111827)
✅ 内部高光 (rgba(255,255,255,0.05))
✅ 多层阴影增强立体感
✅ 圆角 (0.5rem)
```

**CSS**:
```css
.timeline-container {
  background: linear-gradient(to bottom, #1f2937, #111827);
  border-radius: 0.5rem;
  box-shadow: 
    0 4px 6px -1px rgba(0, 0, 0, 0.3),
    0 2px 4px -1px rgba(0, 0, 0, 0.2),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);
}
```

---

### 2. 时间刻度

#### 优化前
```
00:00        00:10        00:20
  │            │            │
  
颜色: #9ca3af (中灰)
问题: 在深色背景下不够清晰
```

#### 优化后
```
00:00        00:10        00:20
  ┃            ┃            ┃
  
颜色: #9ca3af (浅灰) / #d1d5db (文字)
改进: 在深色背景下清晰可见
```

**对比度分析**:
| 元素 | 前景色 | 背景色 | 对比度 | WCAG |
|------|--------|--------|--------|------|
| 主刻度 | #9ca3af | #1f2937 | 4.8:1 | AA ✅ |
| 标签 | #d1d5db | #1f2937 | 9.2:1 | AAA ✅ |

---

### 3. 时间轴轨道

#### 优化前
```
┌─────────────────────────────────────┐
│ 纯色 #374151                        │
└─────────────────────────────────────┘

问题：
❌ 平面，无立体感
❌ 无内部细节
❌ 悬停无反馈
```

#### 优化后
```
╔═════════════════════════════════════╗
║ 渐变 #374151 → #1f2937              ║
║ + 内部高光 + 内阴影                 ║
╚═════════════════════════════════════╝

改进：
✅ 深色渐变背景
✅ 内部高光 (rgba(255,255,255,0.05))
✅ 内阴影 (rgba(0,0,0,0.3))
✅ 悬停时背景变亮 + 高光增强
```

**CSS**:
```css
.timeline-track {
  background: linear-gradient(to bottom, #374151, #1f2937);
  border: 1px solid #4b5563;
  box-shadow: 
    inset 0 2px 4px rgba(0, 0, 0, 0.3),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);
}

.timeline-track:hover {
  background: linear-gradient(to bottom, #3f4a5a, #252f3f);
  box-shadow: 
    inset 0 2px 4px rgba(0, 0, 0, 0.4),
    inset 0 1px 0 rgba(255, 255, 255, 0.08);
}
```

---

### 4. 裁剪区域遮罩

#### 优化前
```
████████████████████████████████████
[半透明黑色 rgba(0,0,0,0.4)]
████████████████████████████████████

问题：
❌ 在深色背景下不够明显
❌ 无纹理，单调
```

#### 优化后
```
╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱
[深色斜纹图案 + 模糊]
╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱╱

改进：
✅ 深色斜纹图案 (rgba(0,0,0,0.4) ↔ rgba(0,0,0,0.6))
✅ 模糊效果 (backdrop-filter: blur(1px))
✅ 激活区域更亮的蓝色高亮
✅ 更明显的边框 (rgba(59,130,246,0.7))
```

**CSS**:
```css
.trim-overlay {
  background: repeating-linear-gradient(
    45deg,
    rgba(0, 0, 0, 0.4),
    rgba(0, 0, 0, 0.4) 10px,
    rgba(0, 0, 0, 0.6) 10px,
    rgba(0, 0, 0, 0.6) 20px
  );
  backdrop-filter: blur(1px);
}

.trim-active-region {
  background: linear-gradient(
    to bottom, 
    rgba(59, 130, 246, 0.25), 
    rgba(37, 99, 235, 0.35)
  );
  border-top: 2px solid rgba(59, 130, 246, 0.7);
  border-bottom: 2px solid rgba(59, 130, 246, 0.7);
}
```

---

### 5. Zoom 控制区

#### 优化前
```
┌─────────────────────────────────┐
│ 🔍 Click and drag to zoom       │
│ [透明背景，低对比度]            │
└─────────────────────────────────┘

问题：
❌ 与背景融为一体
❌ 不够醒目
```

#### 优化后
```
╔═════════════════════════════════╗
║ 🔍 Click and drag to zoom       ║
║ [半透明深色背景 + 虚线边框]    ║
╚═════════════════════════════════╝

改进：
✅ 半透明深色背景 rgba(31,41,55,0.5)
✅ 虚线边框 #4b5563
✅ 悬停时浅蓝色高亮 #60a5fa
✅ 悬停时浅蓝色背景 rgba(37,99,235,0.1)
```

**CSS**:
```css
.zoom-hint {
  background: rgba(31, 41, 55, 0.5);
  border: 1px dashed #4b5563;
  color: #9ca3af;
}

.zoom-hint:hover {
  color: #60a5fa;
  border-color: #60a5fa;
  background: rgba(37, 99, 235, 0.1);
}

.zoom-mini-timeline {
  background: linear-gradient(to bottom, #374151, #1f2937);
  border: 1px solid #4b5563;
  box-shadow: 
    inset 0 2px 4px rgba(0, 0, 0, 0.3),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);
}
```

---

## 🎯 深色模式设计原则

### 1. 对比度优先
- 文字与背景对比度 ≥ 7:1 (AAA 级)
- 重要元素对比度 ≥ 4.5:1 (AA 级)
- 使用浅色文字 (#d1d5db, #9ca3af)

### 2. 层次感
- 使用渐变背景增加深度
- 使用内部高光模拟光照
- 使用阴影分离层级

### 3. 视觉反馈
- 悬停时背景变亮
- 悬停时高光增强
- 拖拽时缩放 + 发光

### 4. 色彩协调
- 主色调：深灰系 (#1f2937, #374151)
- 强调色：蓝色系 (#60a5fa, #3b82f6)
- 警示色：红色系 (#ef4444, #dc2626)

---

## 📐 关键尺寸

| 元素 | 尺寸 | 说明 |
|------|------|------|
| 容器内边距 | 1rem | 呼吸空间 |
| 轨道高度 | 2.5rem | 易于点击 |
| 手柄尺寸 | 2.5rem | 易于拖拽 |
| 播放头宽度 | 3px | 清晰可见 |
| 主刻度粗细 | 2px | 醒目 |
| 圆角 | 0.375rem | 柔和 |

---

## 🌈 颜色参考表

### 背景色
```css
--bg-container: linear-gradient(#1f2937, #111827)
--bg-track: linear-gradient(#374151, #1f2937)
--bg-zoom: rgba(31, 41, 55, 0.5)
```

### 边框色
```css
--border-default: #4b5563
--border-hover: #6b7280
--border-active: #60a5fa
```

### 文字色
```css
--text-primary: #d1d5db
--text-secondary: #9ca3af
--text-muted: #6b7280
```

### 强调色
```css
--accent-blue: #60a5fa
--accent-blue-dark: #3b82f6
--accent-red: #ef4444
--accent-green: #10b981
```

---

## 🧪 测试清单

### 视觉测试
- [ ] 时间刻度在深色背景下清晰可见
- [ ] 裁剪遮罩斜纹图案明显
- [ ] 手柄在深色背景下易于识别
- [ ] 播放头发光效果明显
- [ ] Zoom 控制区与背景有明显区分

### 对比度测试
- [ ] 所有文字对比度 ≥ 7:1 (AAA)
- [ ] 所有图标对比度 ≥ 4.5:1 (AA)
- [ ] 边框对比度 ≥ 3:1

### 交互测试
- [ ] 悬停状态反馈明显
- [ ] 拖拽状态视觉清晰
- [ ] 动画流畅自然
- [ ] 禁用状态明显

---

## 📝 使用示例

### 在深色页面中使用
```svelte
<div class="bg-gray-800 p-6">
  <Timeline
    timelineMaxMs={60000}
    currentTimeMs={15000}
    isPlaying={true}
    trimEnabled={true}
    trimStartMs={10000}
    trimEndMs={50000}
    onSeek={handleSeek}
    onTrimStartChange={handleTrimStart}
    onTrimEndChange={handleTrimEnd}
  />
</div>
```

### 测试页面
访问 `/test-timeline-optimized` 查看深色模式效果

---

## ✨ 总结

深色模式优化重点：
1. ✅ 使用深色渐变背景与页面协调
2. ✅ 使用浅色文字和图标保持可读性
3. ✅ 使用内部高光增强立体感
4. ✅ 使用深色斜纹图案增强裁剪区域识别度
5. ✅ 使用半透明背景和虚线边框区分 Zoom 控制区
6. ✅ 保持高对比度 (AAA 级)
7. ✅ 提供丰富的视觉反馈

优化后的 Timeline 组件在深色模式下既美观又实用，完美融入 VideoPreviewComposite 的深色界面！

