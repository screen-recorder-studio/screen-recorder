请端到端评估**视频编辑端（Studio）**，重点关注其如何“消费”录制数据并维护预览与导出的一致性：

1、编辑器主控：@src/routes/studio/+page.svelte (数据流调度与窗口管理)
2、存储读取器：@src/lib/workers/opfs-reader-worker.ts (对录制索引的非线性解析)
3、混合预览组件：@src/lib/components/VideoPreviewComposite.svelte (混合渲染逻辑)

评估重点：
- **录制契约解析**：评估 Studio 如何处理录制端生成的 `index.jsonl`。当录制端因为性能波动产生非等间距时间戳时，Studio 的预览是否能保持音画同步，不产生抖动？
- **GOP 对齐算法适配**：分析 `computeFrameWindow` 如何根据录制端定义的关键帧位置进行 Seek。是否存在因 GOP 跨度超过窗口大小导致的解码失败？
- **装饰参数的坐标映射**：背景颜色、圆角、阴影等 Svelte 状态是以什么坐标系存储的？评估这些参数在不同屏幕分辨率预览时，如何映射到录制视频的原始像素坐标，以备导出端调用。
- **时间精度转换**：检查录制端的微秒（μs）如何映射到 Studio 的毫秒（ms）时间轴。评估在高倍率缩放时间轴时，是否存在精度丢失导致的帧跳变。
- **内存回收机制**：在消费录制端产生的海量二进制块时，如何确保不再需要的 Chunks 被及时从内存卸载。
