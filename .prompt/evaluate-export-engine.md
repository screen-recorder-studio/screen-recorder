请端到端评估**视频导出（Export）**功能，重点关注其如何将“录制原片”与“编辑参数”合成为最终成品：

1、导出控制面板：@src/lib/components/VideoExportPanel.svelte (参数聚合入口)
2、离屏合成逻辑：评估其如何从 OPFS 读取原帧并在 OffscreenCanvas 应用 Studio 的装饰参数（背景/圆角/阴影）。
3、编码封包引擎：WebCodecs 二次编码与 Muxer 封包。

评估重点：
- **参数合成保真度**：分析导出端如何获取录制端的原始像素数据。在叠加 Studio 的“圆角”和“阴影”时，是否使用了硬件加速（如 GPU 渲染）？导出的视频在文字边缘是否维持了录制端的清晰度？
- **输出规格一致性**：评估导出端是否遵循了录制端的原始 FPS 和色彩参数。是否存在录制时是 60fps，导出却因逻辑限制降为 30fps 的降级风险？
- **大文件 I/O 链路**：录制端是“分块写”，导出端通常是“顺序读 -> 转码 -> 顺序写”。评估在导出 4K 视频时，磁盘 I/O 是否会成为瓶颈，系统如何通过 WritableStream 防止内存溢出。
- **MP4 兼容性终结**：检查 Muxer 产生的 MP4 文件是否包含完整的 metadata，确保录制端采集到的每一毫秒在各种播放器中都能被准确 seek。
- **License 强制校验**：评估在导出这一最终环节，License 限制（分辨率/水印）是否与 UI 状态及底层编码器配置严丝合缝。